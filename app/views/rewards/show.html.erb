<div class="jumbotron">
  <h1>Congratulations!</h1>
  <p>You just claimed a reward from <%= @merchant.name %>.</p>

  <p>You have claimed <strong><%= @reward_count %></strong> meals of <%= @merchant.meals_required %> and need <%= @merchant.meals_required - @reward_count %> more to claim $<%= @merchant.cash_prize %>!</p>
  <div class="progress">
    <div class="progress-bar progress-bar-success progress-bar-striped active" role="progressbar" aria-valuenow=<%= @reward_count %> aria-valuemin="0" aria-valuemax=<%= @merchant.meals_required %> style="width: <%= @reward_percentage_complete %>%">
    </div>
  </div>
  <p>
    <%= link_to "Back to Home", root_path, class: "btn btn-primary btn-lg" %>
    <% if @reward_count >= @merchant.meals_required %>
      <%= button_to "Claim your gift card!", merchant_gift_cards_path(@merchant), class: "btn btn-success btn-lg"  %>
    <% else %>
      <%= link_to "Claim Another Meal", merchants_path, class: "btn btn-primary btn-lg" %>
    <% end %>
  </p>
</div>
